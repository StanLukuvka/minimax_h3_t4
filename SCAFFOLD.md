# Scaffold provenance

This repository was generated from the official ComfyUI custom-node extension scaffold:

- Template: `Comfy-Org/cookiecutter-comfy-extension`
- Template commit: `cf4077f11804e009e15b7b399a1b062e317c77e6`
- Generation date: 2026-08-07
- Backend-only selection: `frontend_type=no`
- Package/Registry slug: `minimax_h3_t4`
- License selection: GNU General Public License v3

The host did not have `comfy-cli` installed, so the documented `comfy node scaffold` operation was performed directly with the exact official cookiecutter template that command wraps. The generated project structure, CI, Registry metadata section, tests, lint configuration, manifest, and node registration layout are retained as the package foundation.
